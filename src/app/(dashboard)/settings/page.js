import ProfileSettings from '@/components/settings/ProfileSettings';
import React from 'react';

const page = () => {
    return (
        <div>
          <ProfileSettings/>
        </div>
    );
};

export default page;